import styled from 'styled-components'
import { clearFix } from 'polished'

export default styled.nav`
  height: 100%;
  font-size: 30px;
  padding: 5px;
  width: 40px;
  float: left;
  ${clearFix()}
`
